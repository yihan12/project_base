import { product } from "../index"
const feature = "platformSku"

const platformSkuDetail = `/${product}/${feature}/detail`
const platformSkuSearch = `/${product}/${feature}/search`

module.exports = {
  platformSkuDetail,
  platformSkuSearch,
}
