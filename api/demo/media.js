import { product } from "../index"
const feature = "media"

const mediaFind = `/${product}/${feature}/find`
const mediaFindWeApp = `/${product}/${feature}/findWeApp`

module.exports = {
  mediaFind,
  mediaFindWeApp,
}
