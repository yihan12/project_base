import { product } from "../index"
const feature = "category"

const categoryFind = `/${product}/${feature}/find`
const categoryFindWeApp = `/${product}/${feature}/findWeApp`

module.exports = {
  categoryFind,
  categoryFindWeApp,
}
