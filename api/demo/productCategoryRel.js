import { product } from "../index"
const feature = "productCategoryRel"

const productCategoryRelFind = `/${product}/${feature}/find`
const productCategoryRelFindWeApp = `/${product}/${feature}/findWeApp`

module.exports = {
  productCategoryRelFind,
  productCategoryRelFindWeApp,
}
